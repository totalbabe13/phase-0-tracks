
// 7.1 Leonardo Kaplan
//- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 



// obtain a string 
var not_rev_word = ""; // <-- variable created to hold given string

//can you point to individual characters in string?
charAt()

// are there built in Java functions? (like ruby?)
.split(); // --> separates string characters into individual objects
.substring() // --> RETURNS new string where charcters are missing, uses index num range as parameter
"word or string".length // --> gives numeric length of characters (same as ruby)
.pop()//    removes last element of array
.push()//   adds element to end of array
.join()//	Joins all elements of an array into a string.

//how to put data into an array and manipulate order?

//comparisons in java 
(word === "") // equal value and equal type?
// can you use IF statments? -->  if (str === "")


// source data from array--> convert to string (remember that you can add string in JS?)

//RETURN a reverse string